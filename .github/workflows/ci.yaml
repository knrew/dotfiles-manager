name: CI

on:
  push:
    branches:
      - master
      - main
  pull_request:
  schedule:
    - cron: 0 0 1 * *
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUST_CACHE_SAVE_IF: ${{ github.event_name == 'pull_request' || github.event_name == 'schedule' || github.ref_name == github.event.repository.default_branch }}

jobs:
  taplo:
    name: taplo
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Check TOML formatting
        uses: docker://docker.io/tamasfe/taplo:0.10.0
        with:
          args: fmt --check --diff

  yamllint:
    name: yamllint
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    env:
      YAMLLINT_VERSION: 1.38.0
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: 3.14
      - name: Install yamllint
        run: python -m pip install "yamllint==${YAMLLINT_VERSION}"
      - name: Check YAML formatting
        run: yamllint -f github .

  shell-lint:
    name: shell-lint
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    env:
      SHELLCHECK_VERSION: 0.11.0
      SHFMT_VERSION: 3.13.1
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Install ShellCheck
        run: |
          curl -fsSL \
            -o /tmp/shellcheck.tar.xz \
            "https://github.com/koalaman/shellcheck/releases/download/v${SHELLCHECK_VERSION}/shellcheck-v${SHELLCHECK_VERSION}.linux.x86_64.tar.xz"
          tar -C /tmp -xf /tmp/shellcheck.tar.xz
          sudo install -m 0755 "/tmp/shellcheck-v${SHELLCHECK_VERSION}/shellcheck" /usr/local/bin/shellcheck
      - name: Install shfmt
        run: |
          curl -fsSL \
            -o /tmp/shfmt \
            "https://github.com/mvdan/sh/releases/download/v${SHFMT_VERSION}/shfmt_v${SHFMT_VERSION}_linux_amd64"
          sudo install -m 0755 /tmp/shfmt /usr/local/bin/shfmt
      - name: Check shell scripts
        run: bash .github/scripts/lint-sh-bash.sh

  markdownlint:
    name: markdownlint
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Check Markdown formatting
        uses: DavidAnson/markdownlint-cli2-action@v24
        with:
          config: .markdownlint.yaml
          globs: "**/*.md"

  cargo-fmt:
    name: cargo-fmt
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Cargo fmt
        run: cargo fmt --all --check --verbose

  cargo-clippy:
    name: cargo-clippy
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4  # v2.9.1
        with:
          save-if: ${{ env.RUST_CACHE_SAVE_IF }}
      - name: Cargo clippy
        run: cargo clippy --workspace --all-features --all-targets

  cargo-check:
    name: cargo-check (${{ matrix.profile }})
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - profile: debug
          - profile: release
            release_flag: --release
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4  # v2.9.1
        with:
          key: ${{ matrix.profile }}
          save-if: ${{ env.RUST_CACHE_SAVE_IF }}
      - name: Cargo check
        run: cargo check ${{ matrix.release_flag }} --workspace --all-targets --all-features --verbose

  cargo-test:
    name: cargo-test (${{ matrix.profile }})
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - profile: debug
          - profile: release
            release_flag: --release
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4  # v2.9.1
        with:
          key: ${{ matrix.profile }}
          save-if: ${{ env.RUST_CACHE_SAVE_IF }}
      - name: Cargo test
        run: cargo test ${{ matrix.release_flag }} --workspace --all-features --no-fail-fast --verbose
